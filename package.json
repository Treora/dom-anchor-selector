{
  "name": "dom-anchor-selector",
  "version": "0.1.0",
  "description": "Create selector objects to refer to DOM Ranges, and vice versa",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "dom-anchor-text-quote": "^4.0.2"
  }
}
